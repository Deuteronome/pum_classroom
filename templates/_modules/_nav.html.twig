<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <p class="navbar-brand m-0" href="#">{{ pageInfo.title}}</p>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_dashboard')}}">Tableau de bord</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_account', {'id' : app.user.id}) }}">Compte</a>
                    </li>
                    {% if (app.user.roles[0] == 'ROLE_TEACHER' or app.user.roles[0] == 'ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle active" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Espace Enseignant
                            </a>
                            <ul class="dropdown-menu">                
                                <li><a class="dropdown-item" href="{{ path('app_new_group')}}">Création de classe</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_group_list')}}">Gestion des classes</a></li>
                                <li><a class="dropdown-item" href="#">Gestion des étudiants</a></li>
                                <li><a class="dropdown-item" href="#">Gestion des contenus</a></li>
                            </ul>
                            
                        </li>
                        {% if app.user.roles[0] == 'ROLE_ADMIN' %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle active" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Espace Administrateur
                            </a>
                            <ul class="dropdown-menu">                
                                <li><a class="dropdown-item" href="{{ path('app_user_list')}}">Gestion des comptes</a></li>
                                
                            </ul>                            
                        </li>
                        {% endif %}
                    {% endif %}         
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_logout')}}">Déconnexion</a>
                    </li>  
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_login')}}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_forgotten_password')}}">Mot de passe oublié</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_new_user')}}">Inscription</a>
                    </li>
                {% endif %}            
            </ul>
        </div>
    </div>
</nav>