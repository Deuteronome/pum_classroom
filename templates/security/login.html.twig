{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block main %}
    <main class="container-fluid p-5">
        <div class="row justify-content-between align-items-stretch">
            <div class="col-12 col-lg-7 box-custom mb-5 mb-lg-0 d-flex flex-column justify-content-center">
                <div>
                    <form method="post">
                        {% if error %}
                            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}            
                        
                
                        <h2 class="mb-3 bold">Indentifiez-vous : </h2>
                        <label for="username" class="form-label">Email</label>
                        <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control mb-2" autocomplete="email" required autofocus>
                        <label for="password" class="form-label">Password</label>
                        <input type="password" name="_password" id="password" class="form-control mb-2" autocomplete="current-password" required>
                
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                
                        {#
                            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                            See https://symfony.com/doc/current/security/remember_me.html
                
                            <div class="checkbox mb-3">
                                <label>
                                    <input type="checkbox" name="_remember_me"> Remember me
                                </label>
                            </div>
                        #}
                
                        <button class="btn btn-lg btn-custom my-4" type="submit">
                            Connexion
                        </button>
                
                        <a href="{{ path('app_forgotten_password')}}" class="btn btn-sm btn-outline-custom mx-4 bold">Mot de passe oublié</a>
                    </form>
                </div>
                
            </div>
            <div class="col-12 col-lg-4 box-custom">
                <h2 class="h3 mb-3 bold">Bienvenue sur la plateforme E2C Apprendre!</h2>
                <p class="p-custom">Cette plateforme vous permettra de suivre votre progression dans les activités proposées à l'E2C. Collectez des badges, validez des évaluations et atteignez vos objectifs pour évoluez.</p>
                <p class="p-custom">Dans un premier temps prévue pour le parcours Pépa Numérique, son expérimentation permettra d'envisager son utilisation à d'autres parcours.</p>
                <p class="p-custom">Si vous êtes un stagiaire de l'E2C, vous aurez besoin d'un code de groupe pour vous inscrire. Si votre formateur vous en a donné un, cliquez pour crééer votre compte :</p>
                <a href="{{ path('app_new_user')}}" class="btn btn-lg btn-custom my-4">Inscription</a>
            </div>
        </div>
        
    </main>
    
{% endblock %}
